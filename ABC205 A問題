#2021-06-13 A問題

# 100 mL あたり A kcal
# このドリンク B mL は xx kcal？
# 0 <= A, B <= 1000

#####################
#ここに入力例を入れる
puts "100mL あたりのカロリー A [kcal] とドリンクの量 B [ml] を入力してください"
A, B = gets.split.map(&:to_f)
#to_i はInterger（整数）型　to_f  はfloat（浮動小数）型
#####################

puts A * B / 100
